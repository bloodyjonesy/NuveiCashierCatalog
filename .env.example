# Nuvei sandbox (demo) credentials — used when "Use demo credentials" is selected
# Stored server-side only; never exposed to client
NUVEI_MERCHANT_ID=6505371860607581795
NUVEI_MERCHANT_SITE_ID=231378
NUVEI_SECRET_KEY=

# Payment page base URL (default: sandbox). Use production only with production credentials.
# Sandbox: https://ppp-test.safecharge.com/ppp/purchase.do
# Production: https://secure.nuvei.com/ppp/purchase.do
NUVEI_PPP_BASE_URL=https://ppp-test.safecharge.com/ppp/purchase.do
NEXT_PUBLIC_NUVEI_PPP_BASE_URL=https://ppp-test.safecharge.com/ppp/purchase.do

# App URL for notify_url (e.g. https://your-app.railway.app)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: PostgreSQL for theme catalog persistence across deploys (e.g. Railway Postgres).
# When set, themes (theme_id, name, screenshot link/base64) are stored in the DB instead of data/themes.json.
# Railway: link the Postgres service to your app; it sets DATABASE_URL or DATABASE_PRIVATE_URL.
# Both are supported — use the variable Railway provides (often DATABASE_PRIVATE_URL for in-network).
# DATABASE_URL=postgresql://...
